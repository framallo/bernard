%header.navbar.navbar-inverse
  %nav.navbar-inner
    .container

      = link_to 'Bernard', root_path, class:'brand'

      .nav-collapse.collapse
        %ul.nav
          %li= link_to 'All transactions', transactions_path
          = render '/accounts/menu'
          %li=link_to 'Budgets', budgets_path
          %li= link_to 'Import', import_index_path


        = form_tag(search_path, method: :get, class: 'navbar-search pull-right') do
          = text_field_tag 'q', params[:q], placeholder: 'Search', class: 'search-query'
