%table.table-bordered
  %thead
    %tr
      %th Type
      %th Amount

  %tbody
    - @report.each do |pm_type, amount|
      %tr
        %td= pm_type
        %td= amount

%canvas#canvas(height="450" width="600")
