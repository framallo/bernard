= render 'menu'
%h1 Spending by Payee Report
%table.table-bordered
  %thead
    %tr
      %th Payee Id
      %th Amount
  %tbody
    = render partial: "table_body", locals: {report: @report}

%canvas#canvas{height: '450', width: '600'}
= javascript_include_tag 'reports/spending_by_payee'
